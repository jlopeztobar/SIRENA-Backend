package co.edu.unicauca.SIRENABackend.security.common.enums;

/**
 * Enumeración que define tipos de token utilizados en autenticación.
 * En este caso, solo se define el tipo 'Bearer', que es comúnmente utilizado con tokens JWT.
 */
public enum TokenTypeEnum {
    BEARER
}
